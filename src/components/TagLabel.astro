---
interface Props {
    tag: string | undefined;
}

let { tag } = Astro.props;
const href = `/blog/tag/${tag}`;
---

{ (tag !== undefined) &&
    <p class="tag">
        <a href={href} data-astro-prefetch>
            <small>{tag}</small>
        </a>
    </p>
}
